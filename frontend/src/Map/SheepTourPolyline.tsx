import { Polyline } from "./Polyline"
import { Tour } from "../Types/Tour"

export const SheepTourPolyline = (props: {tours: Tour[], map: any, maps: any}) => {
  //const path = props.tour.
  return(
    <div></div>
   
  )
}

/*
 <Polyline
      map={props.map}
      maps={props.maps}
      path={path2} />

*/